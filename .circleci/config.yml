version: 2.1
jobs:
  skip:
    working_directory: ~/emma-sax4.github.io
    docker: [ image: circleci/ruby:2.6.5 ]
    steps: [ checkout ]
workflows:
  version: 2
  BRANCH_TO_IGNORE:
    jobs:
      - skip:
          filters:
            branches:
              ignore: [ master ]
